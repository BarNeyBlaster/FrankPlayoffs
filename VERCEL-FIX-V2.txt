Vercel build fix v2

vite.config.js is now a clean Vite React config. The old Base44 plugin invocation
was removed entirely.

Vercel settings:
Build command: npm run build
Output directory: dist
Framework preset: Vite

Environment variables:
VITE_SUPABASE_URL=https://dqkgbaaluhamtgxdmyse.supabase.co
VITE_SUPABASE_ANON_KEY=<your publishable/anon key>

If Vercel still shows the old base44Compat error after deploying this ZIP, the
GitHub repository is deploying an older commit. Replace the repository contents
with this ZIP or push a new commit, then deploy the new commit in Vercel.
